.build/
.swiftpm/
*.xcodeproj

# Claude Code / agent tooling — local only, never commit
CLAUDE.md
.claude/
.codegraph/
.impeccable/

# Distributable artifacts
dist/

# Node / editor build
node_modules/
.DS_Store
vsc.code-workspace

# .NET — Windows port under windows/ (isolated from the SwiftPM app)
windows/**/bin/
windows/**/obj/
*.user

# Downloaded model weights — the real download target is Application
# Support (ModelCatalog.modelsDirectory), never the repo.
*.gguf
gguf/
.vscode/
